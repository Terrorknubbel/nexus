export type ThemeType = typeof light;

export const light = {
  background: '#121212',
  primary: '#6611b5',
  onPrimary: '#d7adff',
  surface: '#1E1E1E'
}

const theme = light;
export default theme;
